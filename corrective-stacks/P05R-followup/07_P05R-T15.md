# P05R-T15 — Replace false-positive gate evidence with production-path regressions

Depends on: T14.
Closes: R10 and consolidated verification of R01–R09.
Commit prefix: P05R-T15:

## Required patch

1. Integrate T09–T14 regressions into the normal general/security scripts. Keep useful existing tests; rename misleading tests and remove wrong-endpoint assertions that passed only because a route was absent.
2. Test concurrency through the actual transactional create unit used by the editor. Each competitor creates its own successor inside its attempt. No precommitted candidate documents and no sequence of two awaited calls presented as a race.
3. Coordinate two independent requests/connections or processes with a start barrier. Log that both attempts were launched before release and record outcomes. Respect SQLite locking: do not deadlock by requiring both to hold a write lock before proceeding. Lock/serialization conflict may reject or trigger bounded whole-operation retry, but never partial commit.
4. Assert exactly one complete success for the same predecessor, one edge, one legitimate lock transition, one successor with required credit/provenance, and no losing successor, metadata, Character, or audit/provenance residue. A loser must receive a stable failure with retained fields. Repeat with independent fixtures to expose timing-sensitive failures.
5. Retain injected-failure tests AFTER each real aggregate mutation, including actual lifecycle update and required credit; do not assert rollback of a write that was never performed.
6. Verify real version-list/detail REST endpoints and Local API, both positive and hostile cases. Assertions must distinguish denial from route-not-found.
7. Gate includes large permission identity/removal fixtures, audit-storage failures, and old-schema upgrade tests. Do not omit them from scripts because they are slower; a dedicated named integration command is acceptable if the gate runs it.
8. Separate pure/helper, source/static, integration, and rendered UI evidence. Test People keyboard selection, visible failure feedback, and editor field preservation through an existing browser harness if available. Do not claim helper/source tests prove rendered behavior. If no suitable harness exists, record executable browser checks in GATE2 instead of adding a framework without need.
9. Create execution-notes/P05R-T15.md with a finding-to-test map (file + test name), fresh commands/counts and limitations. Update current continuation pointers so P06 is blocked by GATE2; preserve earlier notes as historical.
10. Preserve named deferrals and T08 changes. If packet text changes, regenerate SHA256SUMS and validate with 84 tickets; never add follow-up tickets to that fixed directory.

## Required evidence

- Actual test output and exits from npm test, npm run test:security (plus any new integration command), npx tsc --noEmit, npm run lint.
- Fresh production build against an explicitly isolated DB with a controlled output/server lifecycle.
- Regenerate Payload types and verify no unexplained generated diff remains.
- Packet validator PASS and hash checks.
- Real DB schema verification command from T14 PASS.
- No skipped/focused tests that hide mandatory cases; each R01–R10 row names its definitive assertion.

## Done when

Evidence supports the actual claims rather than just green counts. All defects remain closed on the final code. Leave owner UI acceptance and CC-2026-09-03-05 approval explicitly pending for GATE2; do not self-approve them.

