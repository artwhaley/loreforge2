# P1-T07 — Compile-time drop-in proof

- Repository: `sl-civic-archive`
- Branch: `patch/design-contract-management-and-obsidian`
- Start/end commit: `a49cabed490576589a887734768e06c7b8d6680b` (working tree execution; no commit created)

## Fixture

Added `test-fixtures/designs/portability-probe/`, a minimal full-contract package with a hyphenated key. The fixture uses only the portable host surface and includes a bundled thumbnail.

## Script

Added `scripts/test-design-dropin.mjs` with `try/finally` cleanup. It installs the fixture temporarily, runs discovery/materialization, asserts generated keys/catalog/registry and asset output, typechecks, optionally builds, removes the fixture, reruns discovery, and asserts all probe outputs are gone.

## Verification

- `npm run test:design-dropin` passed.
- `npm run test:design-dropin:build` passed with the probe installed; the full Next production build completed and the probe was removed afterward.
