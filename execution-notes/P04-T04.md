# P04-T04 execution note

- Scope: Draft submission, direct filing, supervisor approval/rejection, explicit lock/unlock, review queue, and reversible soft-delete/restore.
- Automated checks: `npm test` passed (57 tests); `npx tsc --noEmit` passed. Seed completed against the local SQLite fixture with `PAYLOAD_PUSH=false` after the new tables/indexes were initialized.
- Interim authority: submit is Character-scoped for ordinary members; file/approve/reject/lock/unlock and both restore paths require the P03 `authorizeInterimOperation` owner/active operational Domain Admin boundary.
- Data invariant: ordinary listings/search exclude `softDeletedAt`; history can still retrieve a deleted record by exact Domain + Document and restore preserves its ID, revisions, and provenance.
- Manual acceptance: deferred to the Phase 4 gate. Human checks must walk clerk submit → supervisor approve/reject, pending/locked read-only behavior, lock/unlock, and soft-delete/restore including the timeline.
