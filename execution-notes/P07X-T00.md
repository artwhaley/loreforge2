# P07X-T00 — freeze acting identity and document workflow redirects

**Branch:** `phase-07x-acting-identity-document-workflows` (created from the fully green Phase 7 head `5e3c4d1`).

## What changed

Canonical packet docs patched so later agents cannot restore the superseded model:

- `02_FROZEN_PRODUCT_DECISIONS.md` — four hard-separated Character kinds; acting identity authoritative; selector semantics include provisioned administrative identities; exactly-one Domain owner/admin (synonyms: owner / Domain Admin / Domain owner); Document-Type-primary record permissions; Type-first creation with lifecycle routing; platform administration as an acting identity.
- `03_ARCHITECTURE_CONTRACT.md` — Section 5 rewritten as "Acting identity and authority seams"; operating-context bar contract updated; PermissionRule resource vocabulary now includes `DocumentType`; document-types model gains creation methods + lifecycle Folder routing; authorization precedence reworked into the two-axis decision; filing-policy precedence replaced by Type state routing; templates destination fields marked legacy compatibility; form field vocabulary includes `time`/`characters`; form header/footer contract added.
- `06_CHANGE_CONTROL.md` — `CC-P07X-2026-09-04` entry superseding the User-admin bypass / multi-admin / Folder-primary / Template-destination statements.
- `07_TICKET_INDEX.md` — P07X section listing T00–T11 plus GATE-A/GATE-FINAL.
- `11_DEFERRED_WORK_REGISTER.md` — DEF-ADMIN-01 (retire domain-admins), DEF-TYPE-01 (translate legacy Folder record-capability rules), DEF-TEMPLATE-01 (remove customer Template destination surface).
- `10_PACKET_MANIFEST.md` — reference to the root-level P07X packet.
- `SHA256SUMS.txt` — regenerated after the edits.

## Validation

- `python tools/validate_packet.py` → **PACKET VALIDATION: PASS** (84 tickets, 69 implementation, 15 gates, 15 phases, references complete).
- Guardrail: documentation/scaffolding only; no runtime behavior changed in this ticket.

## Acceptance

A fresh executor reading the canonical docs now understands the four Character kinds, acting-identity authority, one Domain admin, Type-primary permissions, lifecycle Folder routing, invitations, and non-universal queues.