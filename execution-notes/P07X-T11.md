# P07X-T11 — Migration cleanup, fixtures, and integrated acceptance

Status: complete

## Commit

Final commit on `phase-07x-acting-identity-document-workflows`:
`P07X-T11 complete integrated identity workflow correction` (hash reported in
the completion handoff).

## What shipped

- Added an additive, idempotent integrated fixture helper and standalone
  `seed:p07x` entry point. The reserved cast includes the platform Administrator,
  Administrator of Ar, Lucan, Tarl, Marlen, Cassius, NPC Villager, and an
  unclaimed player Character, with Ar Document Types, lifecycle Folders, roles,
  Type rules, Incident Report document/form Templates, and form header/footer.
- Wired the integrated fixture helper into the normal seed so a clean project
  seed contains the same current corrective acceptance data without replacing
  earlier phase fixtures.
- Added `migrate:p07x-t11` with explicit local-SQLite dry-run/apply modes. It
  verifies the P07X schema, backfills Character kinds, provisions missing admin
  identities, reports legacy `domain-admins` incompatibilities and ambiguous
  Folder record-capability mappings, verifies DocumentType PermissionRule
  polymorphism, and preserves Templates/Forms while reporting deprecated
  destination fields. It never silently promotes legacy admins or guesses a
  broad Type grant.
- Hid and made read-only the legacy Template destination fields so ordinary
  customer creation remains Type-routed while migration compatibility remains
  observable to tooling.
- Added the integrated acceptance test covering identity separation, Type
  permission matrix, routing, form composition, Domain bootstrap, and Character
  invitation claiming.
- Updated the README, architecture/frozen-decision/fixture packet contracts,
  ticket status, testing guide, and deferred register. The next activity is
  explicitly the owner-led empty-Domain buildout exercise.

## Verification

- `npm run test:p07x-t11`: 2/2
- `npm run test:p07x-t10`: 5/5
- `npm run test:p07x-t09`: 6/6
- fresh disposable P07X focused sweep: T01 kinds 9/9, T02 identity 9/9,
  T03 Type permissions 7/7, T04 Folder projection 4/4, T05 routing 8/8,
  T06 creation 3/3, T07 form output 7/7, and T08 invitation foundation 5/5
- `npm test`: 115/115
- `npm run test:security`: 32/32
- `npx tsc --noEmit`: clean
- focused ESLint over the P07X sources and touched collection/seed files: clean
- `npm run lint`: 0 errors (2 existing `next/no-img-element` warnings in the
  legacy theme shell)
- `npm run build`: production build completed successfully
- `python LoreForge_Execution_Packet/tools/validate_packet.py`: PASS
- `npm run seed` against disposable `p07x-full-seed-test-20260904.db`: PASS
- `migrate:p07x-t11 -- --dry-run` against the integrated fixture: schema ready,
  8 Characters, 1 platform-admin User, 1 Community Domain, 22 verified Type
  rules, 2 Templates preserved, 0 warnings
- `migrate:p07x-t11 -- --apply` against disposable
  `p07x-t11-apply-20260904.db`: schema migration and report completed with exit
  code 0 and no warnings

## Manual acceptance

The packet's browser-level owner review remains intentionally open at
P07X-GATE-FINAL. Automated acceptance covers the server-side identity,
authorization, routing, form, bootstrap, and Character-invite invariants; the
owner must still exercise the visible acting-identity selectors, Role × Type
surface, workflow folders, Type-first creation, Form Studio, invite management,
and Platform/Domain Work views before authorizing the empty-Domain buildout.

## Deliberate remaining work

- Legacy `domain-admins` rows remain report-only compatibility data until the
  owner final gate and a later cleanup window.
- Legacy Template destination fields remain hidden/read-only compatibility data.
- Document sharing remains deferred under the existing owner decision record.
- No Phase 8 starter-pack implementation was started.

## Addendum (owner empty-Domain buildout exercise)

The owner's end-to-end clean build (Domain `Telnus`) is the DEF-STARTER-01
empty-Domain buildout exercise recorded above: the buildout itself — first
Roles, Folders, Document Types, Departments, and Templates for the Domain —
is being performed manually by the owner during the Telnus run, and its
findings feed the starter-pack scope decision at P08-GATE. No starter pack
was installed or implemented; the register row's owning path is this note.
