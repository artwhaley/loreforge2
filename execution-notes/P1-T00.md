# P1-T00 — Freeze and inventory

## Starting commit

- Repository: `sl-civic-archive`
- Branch: `patch/design-contract-management-and-obsidian`
- HEAD: `a49cabed490576589a887734768e06c7b8d6680b`
- Working tree at freeze: two unrelated untracked database backups; no source edits.

## Execution base

The packet's production oracle is the `sl-civic-archive` checkout. The sibling Lab was observed at:

- Branch: `fix/obsidian-fidelity-remediation`
- HEAD: `b69f7053312c4048171a83768fac6f4803506a57`
- Existing Lab changes: `src/host/IframePreviewPane.tsx`, `src/lab.css`, and `tests/e2e/obsidianBrowser.spec.ts`.

The production source is the current authority; the packet's authoring observations are treated as hypotheses and were rechecked here.

## Installed Design folders

Immediate source folders are `civic`, `ledger`, `obsidian`, and `poster`. There are no manifests yet. Each has an `index.ts`; each definition currently carries its own metadata, while `src/lib/design/catalog.ts` repeats the metadata.

## Current Design contract and registration couplings

- `src/lib/design/types.ts` hard-codes `DesignKey = 'civic' | 'ledger' | 'poster' | 'obsidian'` and repeats the same literals in `isDesignKey`.
- `src/lib/design/registry.ts` statically imports and hand-registers all four definitions, hand-maintains `DESIGN_KEYS`, and derives `DESIGN_METADATA` from the hand-authored catalog.
- `src/lib/design/catalog.ts` hand-maintains four catalog rows and `FIRST_CLASS_DESIGNS`.
- `src/lib/design/config.ts` has a second literal key tuple/fallback validator.
- `src/lib/design/contracts.ts` owns `CivicConfigV1`, `LedgerConfigV1`, `ObsidianConfigV1`, and `PosterConfigV1`; these are Design-owned vocabularies that must move during P1-T04.
- `src/lib/design/conformance.design.test.tsx` and registry tests contain explicit all-Design arrays that will need to derive from generated registry/catalog data when the production contract is generalized.
- Site Studio route code consumes `DESIGNS`/`DESIGN_KEYS` from the registry, so generated registry output must preserve that facade.

## Required slots

The current production `DesignDefinition` requires:

- `Shell`;
- public pages `home`, `records`, `document`, `departments`, `department`, `about`, `lore`;
- operational `work` and `members`;
- management pages `departments`, `folders`, `roles`, `documentTypes`, `people`, `person`, and `invitations`;
- `studio.Editor`;
- config `version`, `defaults`, `validate`, `migrate`, optional `fromLegacy`, and `resolveTheme`.

There is no production `pages.member` slot; the plural `members` slot is the current contract.

## Design-owned assets and named strays

- `public/designs/civic.svg`, `ledger.svg`, `poster.svg`, and `obsidian.svg` are currently outside Design folders and are catalog/config defaults.
- Obsidian's default atmosphere is `/media/obsidian-coastline.png`; its `thin.tsx` also embeds that fallback.
- Design-specific named source exists at `src/components/invitations/ObsidianActions.ts` and `src/components/documentTypes/ObsidianActions.ts`; both require classification/migration under P1-T04.
- Obsidian font files under `public/fonts/obsidian/` are shared presentation assets used by the production app; they are not assumed Design-owned image defaults without tracing their consumers.

## Host-import / endpoint inventory

All four Designs import generic production host code such as page-model types, `@/lib/design/*`, theme/color/font helpers, `next/link`, `@/components/platform/OperatingContext`, shared operational bodies, records workspace/actions, document presentation actions, and shared UI components. Obsidian additionally imports `next/navigation`, Radix/Lucide, people/roles workspace hooks, `@/components/people/PersonAccessTrees`, and the Obsidian invitation action alias.

Static operational calls discovered in Design code include `/api/departments`, `/api/role-assignments`, `/api/invitations/revoke`, `/api/invitations/join-decision`, `/api/character-claims`, `/api/domain-memberships`, `/api/folders`, and `/api/document-types`, plus native forms that use the same production paths. Document and Work actions cross through production server-action props.

These are generic host boundaries to preserve; they are not authorization logic to move into a Design. P1-T06 will produce the machine-readable boundary report for the Lab.

## Commands observed

- Production build: `npm run build`
- Production lint: `npm run lint`
- Design tests: `npm run test:design`
- Full test suite: `npm test`
- TypeScript is included in the production build and in the Lab's `npm run build`.

## Result / next ticket

No runtime source was changed for P1-T00. The inventory is sufficient to proceed with the manifest contract. P1-T01 is safe to begin.

## Ending commit

`a49cabed490576589a887734768e06c7b8d6680b` (note-only change; no commit created).
