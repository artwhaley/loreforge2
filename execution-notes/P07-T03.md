# P07-T03 — Department Role defaults and hierarchy

Status: implemented.

## Delivered

- Role default rule scope is validated against the owning Domain and Folder through `assertRoleDefaultScope`; RoleAssignment remains only Character + Role.
- People assignment availability is now calculated with the shared `canAssignRole` decision rather than a client-only hierarchy approximation.
- Role-derived Department participation and held-role queries are centralized in `src/lib/authz/roles.ts`; no Department membership row is created.
- Assignment records retain the optional acting Character who performed the assignment for audit context.
- Existing fixture data already contains distinct Commander, First Captain, Second Captain, and named Scribe-role branches; no role reuse or Folder-scoped assignments were introduced.

## Verification

- `npm test` — 88 passed, 0 failed.
- `npx tsc --noEmit` — passed.
- Role default same-Domain validation and derived participation tests passed.

Manual assignment and default-access checks are included in `PHASE_06_07_TESTING.md`.
