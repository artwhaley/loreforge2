# P01-T04 execution note

- Commits: `e36c5b63c3bb35304d04e3cdee32b926fc32ae68` and scoped fixture repair `c0221d2045e2823284b16dbc990afdeb51cfa364`
- Branch: `phase-01-editor-theme-safety`
- Files changed: Theme Studio/shell and customer copy, `src/lib/media/validateThemeAsset.ts`, `src/lib/actions/uploadThemeAsset.ts`, `src/lib/actions/saveTheme.ts`, `src/lib/theme/input.ts`, `src/collections/Media.ts`, and regression tests.
- Tests: `npm test` — 32 passed; `npx tsc --noEmit` — passed; `npm run build` — passed.
- Clean-seed check: `npm run seed` completed successfully after rasterizing the four SVG source fixtures to PNG.
- Manual acceptance: not performed in this non-interactive execution pass; preset comparison, dual preview, asset selection, and save feedback require a live browser session.
- Lint: not runnable from the baseline configuration; ESLint 9 fails while loading `eslint.config.mjs` because `core-web-vitals` is undefined. This is recorded as an existing tooling issue and was not changed in this ticket.
- Scope: grouped Studio controls, added responsive layout and live save/upload feedback, retained dual shell/document preview, enforced curated theme input validation, and enforced a server-side JPEG/PNG/WebP decode/re-encode boundary (5 MiB, 4096×4096, no animation or trailing payload). Customer-visible city/department copy is now Domain/Subdomain; schema and route names remain unchanged per the contract.
- Deferred: animation-specific fixture generation and live image uploads/browser checks remain Gate 1 evidence; the validator rejects multi-page metadata when encountered. Form Builder observations are in `execution-notes/P01-FORM-BUILDER-OBSERVATIONS.md`.
