# P05R-T06 — Legacy Surface Containment, Error Feedback, Dead Code, and README

**Branch:** `phase-05-remediation` · **Commit:** see `git log -1` · **Mode:** CLEANUP / USER FEEDBACK

## A. `/domain/*` is now the canonical customer implementation

- Moved all 23 real page implementations (plus the co-located `PeopleSearch.tsx`,
  `RecordsExplorer.tsx` and 9 `.module.scss` files) from `tenant/[slug]/**` to
  `domain/[slug]/**`; the old tenant files became `redirect()` shims
  (`LegacyTenantRedirect`) that forward to the canonical URL. Verified by a production
  `next build`: 46 dynamic routes compile (23 domain + 23 tenant shims).
- Zero live paths construct `/tenant/` URLs: `archive.ts` legacy-org fallback now builds
  `basePath: /domain/${slug}` (the only `/tenant/` construction in `src/`, per grep).
- Negative grep for imports from `@/app/(frontend)/tenant` is clean today and will be
  pinned in the P05R-T07 regression module.

## B. Dead route residue removed

- Deleted `api/switch-administration` (stub for a rejected operating mode; grep-verified
  unreferenced outside itself).
- Deleted the empty `api/subdomain-memberships` directory (no files; no references).
- `api/switch-tenant` **kept** — it is live (homepage domain list and `TenantShell` post
  to it).

## C. Dead rejected-model code

- Deleted `src/collections/SubdomainMemberships.ts` (verified: registered in no config and
  imported nowhere — SolarPro's "deregistration" claim was false; the ticket was cleanup
  of the leftover file). The `subdomain-memberships` API dir was already empty.
- Renamed `getSubdomainMemberships()` → `getDepartmentParticipants()` in
  `src/lib/domains/queries.ts` and all three call sites (Departments index, Department
  page, Members page); doc comment states it derives participation from active
  RoleAssignments.
- Deleted `src/components/archive/CopyMarkdownButton.tsx` + its module (grep-verified
  unreferenced). `DeleteFolderButton` / `ImportSampleButton` are live (Import page) and
  were kept.

## D. Legacy persistence retained with explicit guards

Added "Legacy compatibility only. New product code must not depend on this
field/model." comments to `Tenants`, `Memberships`, the hidden `tenant` fields on
`Documents`/`DomainMemberships`, and `Documents.origin`. Removal remains P10
(`DEF-TENANT-01`, `DEF-ORIGIN-01`). No new code depends on them.

## E. Mutation failures now visibly fail

- New `src/lib/documents/errorCodes.ts`: maps thrown errors to a small stable public
  vocabulary — `credit_required` ("The required Prepared by credit cannot be removed."),
  `character_inactive`, `not_domain_member`, `relationship_failed` ("Invalid supersession
  relationship."), `action_failed` ("Unable to modify this record."). Never stack traces,
  identities, or security-rule details.
- `document-links`, `document-tags`, `document-relationships` routes now redirect to
  `...?error=<code>` on failure instead of silently succeeding.
- `domain/[slug]/documents/[id]` renders the stable message as a `role="alert"` banner
  and still renders the record so the user can retry.

## F. README replaced

Root `README.md` rewritten for LoreForge2: product purpose, canonical Domain/Department/
Role/Document vocabulary, setup, current checks (`npm test`, `npm run test:security`,
`tsc --noEmit`), execution-packet workflow pointer, current status (Phase 5 remediated,
Share deferred, Phase 7 pending), and the old SL Civic Archive MVP linked as history only.

## Automated acceptance evidence

- `next build` passes with both route trees; `/tenant/*` shims redirect to `/domain/*`.
- Grep-clean: no `/tenant/` URL construction, no `switch-administration`/
  `subdomain-memberships` residue, no `CopyMarkdownButton`, no imports from
  `app/(frontend)/tenant` outside the shims, no `getSubdomainMemberships`.
- `tsc --noEmit` clean; `npm test` 78/78; `npm run test:security` 25/25.