# P06-T01 — Template model, inheritance scope, and base composition

**Status: executed; automated evidence recorded.**

## Implemented

- Added the LoreForge-owned `templates` collection with Domain, Document Type,
  availability scope Folder, normal destination Folder, kind, lifecycle policy,
  inheritance, override, active/version, and composition fields.
- Added server-side same-Domain validation, base-template cycle checks, and
  exact-one `{{content}}`/declared-token validation.
- Added deterministic availability and destination helpers in
  `src/lib/templates/resolve.ts` and composition/Markdown-safe token helpers in
  `src/lib/templates/compose.ts`.
- Added the first neutral form-schema type used by form Templates. The adapter
  and runtime migration are completed in P06-T02.

## Evidence

- `node --import tsx --test src/lib/templates/compose.test.ts` — 3 passed.
- `npx tsc --noEmit` — passed.
- Payload types regenerated with `npm run generate:types`.

## Manual acceptance

Not run against the customer UI before unattended execution. The exact clean
user checks are consolidated in `PHASE_06_07_TESTING.md`.

## Commit

`c424eac P06-T01: add template model and composition`

