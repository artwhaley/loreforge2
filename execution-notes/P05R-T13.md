# P05R-T13 — Exhaustive Domain-removal cleanup

**Finding:** R06.
**Commit:** PENDING.

Domain participation cleanup now uses unbounded, non-paginated target enumeration for Roles, RoleAssignments, and direct Character Folder PermissionRules inside the existing transaction. It no longer depends on the former 500-row cap or filters the target after truncating a Domain-wide result.

## Verification

`DATABASE_URI=file:./sl-civic-archive-domain-large-test.db PAYLOAD_PUSH=true node --import tsx --test src/lib/domains/domainRemovalLarge.test.ts`

Result: **1 test, 1 passed, 0 failed**. The isolated fixture created 504 direct rules for one Character and verified zero targeted rules remained after removal. The existing Domain-removal suite remains a separate command/database because each DB-backed file owns and resets its own fixture.

T14 still owns applying the schema and T15 owns fault-injection and overlapping-operation coverage.
