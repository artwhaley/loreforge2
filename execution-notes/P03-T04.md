# P03-T04 execution note

Status: COMPLETE — implementation handoff to P03-GATE.

Implemented institutional Role hierarchy and Character Role assignments.

- Roles belong to a Domain and may optionally belong to a Subdomain; `parentRole` is the immediate superior.
- Model/API invariants reject cross-Domain or cross-Subdomain ownership and cyclic hierarchies.
- Role assignments point to Characters and Roles, retain active dates/status and assigning User/Character fields, and may scope to a Folder branch.
- The interim authority boundary is called for every role/assignment mutation; ordinary members and forged requests cannot create or assign Roles.
- The customer Role manager shows hierarchy, Subdomain, Character, Role, and full Folder-scope breadcrumbs.
- Ar fixtures include Scribe, Warrior, and Magistrate trees; the same Captain Role is scoped to First Platoon and Second Platoon; Aren holds both Warrior and Magistrate Roles.
- Domain roster now displays Domain membership, Subdomain membership, controlling User, local alias, and Role assignments as separate concepts.
- Administration is a distinct account-level selector from the Character selector.

Evidence:

- `npm run generate:types` — pass.
- `npx tsc --noEmit` — pass.
- `npm test` — 49 passing.
- `npm run build` — pass; canonical `/domain/[slug]` and role API/page routes compiled.
- `$env:PAYLOAD_PUSH='false'; npm run seed; Remove-Item Env:PAYLOAD_PUSH` — pass; 9 Roles and 11 active assignments seeded.
- `$env:PAYLOAD_PUSH='false'; npm run migrate:phase3; Remove-Item Env:PAYLOAD_PUSH` — pass; 2 legacy Tenants mapped, 5 documents reconciled, body hashes reported, and canonical-only Ar folders skipped safely.

Manual owner validation remains at the Phase 3 gate. No permission evaluator or P04 work was introduced.
