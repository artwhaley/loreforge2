# P1-T01 — Manifest contract

## Starting commit

`a49cabed490576589a887734768e06c7b8d6680b`

## Files changed

- `src/lib/design/manifest.ts`
- `src/lib/design/manifest.design.test.ts`
- `src/designs/{civic,ledger,poster,obsidian}/design.manifest.json`
- `src/designs/{civic,ledger,poster,obsidian}/index.ts` (default exports)
- `src/designs/{civic,ledger,poster,obsidian}/assets/thumbnail.svg`
- `src/designs/obsidian/assets/atmosphere.png`

## Result

Added the V1 pure-data manifest parser with key/folder identity, contract version, entrypoint, status, bounded metadata, traversal, separator, and referenced-file validation. The parser only reads JSON and filesystem metadata; it never imports a Design entrypoint. All four installed Designs now have valid manifests and default exports. Existing public thumbnails were copied into the folders as authoritative source assets, and Obsidian's existing coastline default was copied into its folder for the upcoming bundled-asset migration.

## Commands/tests

- `npx vitest run src/lib/design/manifest.design.test.ts` — passed, 1 file / 8 tests.

## Deviations / pressure

The packet allows thumbnail assets to be copied during P1-T01 so manifest file-existence validation is real before P1-T05. Runtime URLs remain unchanged until the asset contract ticket.

## Unresolved issues

Design-specific config types still live in generic contracts, discovery/registry are still hand-maintained, and Obsidian still resolves its runtime atmosphere as `/media/obsidian-coastline.png`. Those are later ticket scope.

## Next ticket

P1-T02 is safe to begin.

## Ending commit

`a49cabed490576589a887734768e06c7b8d6680b` (uncommitted working-tree changes; no commit created).
