# P04-T05 execution note

- Scope: one selected Domain, optional eligible acting Character, stable Domain navigation, capability-driven management bar, and canonical Departments routes.
- Automated checks: `npx tsc --noEmit` passed after the route/context changes.
- Manual acceptance: deferred to the Phase 4 gate while People and record-entry shells are being added.
- Compatibility: legacy `/subdomains` and `/subdomains/:slug` routes now redirect to `/departments` equivalents; internal Subdomain collection names remain unchanged.
