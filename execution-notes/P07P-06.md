# P07P-06 — Performance gate, metadata correction, and handoff

Status: implementation and automated handoff complete; owner release gate
remains manual for production-scale p95 sampling (2026-09-04).

Domain home recent records show Document Type plus the actual latest
provenance action date/person. Origin/source badges are not rendered, and
unknown provenance is omitted rather than inferred from Prepared-by. The
Phase 7 guide now names every fixture login, Character, Domain, URL, expected
status, cleanup step, and the distinction between a successful empty People
search (`200 {"results":[]}`) and a forbidden response (`403`).

Verification completed:

- `npm test`: 114 passing.
- `npm run test:security`: 31 passing across access, supersession,
  concurrency, People, and Domain-removal suites.
- `npx tsc --noEmit`: clean.
- `npm run lint`: clean with two pre-existing `<img>` warnings in
  `TenantShell.tsx`.
- `npm run build`: successful production build; 29 static pages and all
  dynamic routes generated.
- `verifyPhase7Http.ts`: 104 checks passing against the live server at
  `http://localhost:3055`.
- disposable `bench:p7p -- --scale small`: completed without touching the
  working DB.

The remaining owner-only step is to run warm production samples (5 warmups,
30 samples) on the release machine and record p50/p95/p99 against the packet's
navigation budgets. This is measurement, not an authorization blocker; no
Phase 8 work is included.
