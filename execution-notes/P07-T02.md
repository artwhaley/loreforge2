# P07-T02 — Server, Payload, and query authorization enforcement

Status: implemented on the Phase 7 branch.

## Delivered

- Replaced the interim owner/Domain-admin helper with the shared P07 evaluator across lifecycle, document editing, archive creation, role/assignment, folder, membership, department, People search, templates, claims, character context, tags, links, and relationship paths.
- Deleted the temporary document-share service and its tests. The Share route remains an inert `share_unavailable` response under CC-2026-09-03-04.
- Removed the legacy User Membership authorization fallback from customer actions; Domain ownership, operational Domain administration, active Character membership, and PermissionRules are the only application decisions.
- Closed direct REST/Admin reads for customer security collections; sanctioned server services use explicit evaluator checks and Local API override only after that check.
- Added the direct, idempotent SQLite migration for `users.is_platform_admin` and canonical claim Domain storage; no Payload destructive schema push is required.
- Evaluator resource lookup now precedes authority bypass, preventing cross-Domain resource IDs from being used by an owner or platform administrator.

## Verification

- `npm run migrate:phase7-authz` — passed against `sl-civic-archive.db`.
- `npm run generate:types` — passed.
- `npm test` — 87 passed, 0 failed.
- `npm run lint` — 0 errors (two existing `next/no-img-element` warnings).
- `npx tsc --noEmit` — passed.
- `src/lib/authorization/accessBoundary.test.ts` — 4 passed, including direct REST/local forged mutation/read checks.

## Manual status

Browser acceptance is covered by the combined Phase 6/7 guide after the remaining role, folder, and delegation tickets. The local server is restarted at the final handoff.
