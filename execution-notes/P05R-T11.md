# P05R-T11 — Canonical PermissionRule identity and scope integrity

**Finding:** R05 and R08.
**Commit:** PENDING.

PermissionRules now carry a server-generated, length-delimited `ruleKey` containing Domain, declared types, polymorphic relation collections/IDs, and capability. It is required and unique at the storage layer; hooks recompute it on every write and reject duplicates. The upsert helper queries the exact key and updates the survivor. Domain resources are checked by their own ID, while all other scoped resources must resolve to the rule Domain.

## Verification

`DATABASE_URI=file:./sl-civic-archive-t11-test.db PAYLOAD_PUSH=true node --import tsx --test src/lib/people/peopleWorkspace.test.ts`

Result: **6 tests, 6 passed, 0 failed**. The suite now tests an Alpha rule targeting Beta's Domain, 101 matching Folder identities, opposing-effect duplicate rejection beyond the former 100-row limit, and deterministic upsert. The isolated database was removed after the run.

The actual development database still requires the non-destructive schema upgrade in T14; no user database was touched by this ticket.
