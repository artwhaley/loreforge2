# P05R-T07 — Consolidated Regression Suite and Deferred-Work Verification

**Commit:** (see git log for `P05R-T07:` commit)
**Branch:** `phase-05-remediation`
**Date:** 2026-09-04

## What this ticket delivered

1. **UX-shell / static invariants regression module** — `src/lib/shellInvariants.test.ts` (owner of `DEF-SHELL-01`), 7 tests:
   - TenantShell exposes exactly one Domain selector and the frozen primary nav order `Home/About/Departments/Records`; no Administration-mode entry point.
   - No source file outside the legacy `/tenant` shim tree (or this module) references the `app/(frontend)/tenant` import path.
   - Every file in `src/app/(frontend)/tenant/**` and `src/app/(frontend)/domain/[slug]/subdomains/**` is a pure, comment-marked redirect shim rendering no shell.
   - Dead route handlers absent: `subdomain-memberships`, `switch-administration`, `document-copies`, `document-moves` (path-level); no Administration-mode vocabulary in non-test code.
   - Customer document view shows Share only as a disabled placeholder citing CC-2026-09-03-04 and exposes no Copy/Move/transfer control.
   - `document-shares` endpoint answers `share_unavailable`/403, imports no Payload mutation machinery, and never invokes the temporary share adapter.
   - Deferred-work register rows all carry a nonempty owning ticket/path, none says only "later", and every `DEF-*` ID is cross-referenced in its owning ticket/path outside the register (incl. the P05R-T00 seeded rows `DEF-SEARCH-02`, `DEF-SHELL-01`, `DEF-CHAR-01`).
   - Persona rendering (logged-out / zero-Character / member / manager / admin / owner / Platform Admin) remains a **manual gate walkthrough** per `DEF-SHELL-01`.
2. **Forward patches** to existing ticket text (13 files; no new ticket files, manifest unchanged at 84 tickets / 15 gates):
   - `P06-T01`: acceptance must re-run Phase 5 gate scenario 2.1 (in-place Acting-as switch preserves editor fields) and the Concerns-chips flow.
   - `P06-T02`: retire permissive legacy Form Builder submission create/read surface; no unrestricted legacy submissions API survives (`DEF-FORM-01`).
   - `P06-T04`: active member Character auto-becomes Prepared-by; Character form fields create Concerns; canonical `sourceKind` only.
   - `P07-T02`: list/search projections hide titles/counts (`DEF-AUTH-01`); superseded-notice/successor metadata obeys readable-resource rules (`DEF-AUTH-02`); interim-seam teardown inventory appendix (`DEF-AUTH-03`); N+1-free hot path (`DEF-PERF-01`); durable audit seam (`DEF-AUDIT-01`).
   - `P07-T03`/`P07-T04`/`P07-T05`: audit seam tagged `DEF-AUDIT-01`.
   - `P07-T01` and `P07-GATE` were already fully forward-patched (capability constant, no move/copy, Share reserved, P07-D01 owner result, no legacy authority); no further edit required.
   - `P08-T02`: `publicEnabled` gates public access; Folder `publicAccess` inheritance honored; no private-ancestor leak (`DEF-PUBLIC-01`).
   - `P10-T01`/`P10-T02`: inventory/remove legacy `Tenants`, `Memberships`, dual Domain/Tenant fallback, `Documents.origin` (`DEF-TENANT-01`, `DEF-ORIGIN-01`).
   - `P10-T04`: operational logs never replace durable LoreForge audit events (P05R-T05 seam).
   - `P12-T01`: Postgres-native indexed search; superseded/soft-deleted Documents filtered from active results (`DEF-FTS-01`, `DEF-SEARCH-02`).
   - `P12-T02`: pagination without hard-coded 100-result lists; permission filtering precedes counts/snippets/facets; no hidden-resource leakage (`DEF-SEARCH-01`).
   - `SHA256SUMS.txt` regenerated for all changed files (128 entries, `./`-prefixed canonical form).

## Test inventory (real counts)

| Suite | Count | Result |
| --- | --- | --- |
| `npm test` (main, incl. new shellInvariants 7) | 85 | pass |
| `test:security` → accessBoundary | 4 | pass |
| `test:security` → supersession | 10 | pass |
| `test:security` → peopleWorkspace | 5 | pass |
| `test:security` → domainRemoval | 6 | pass |
| `npx tsc --noEmit` | — | clean |

The consolidated T07 requirement list maps onto already-green suites from P05R-T01..T06 (authorization bypass, supersession invariants/concurrency, role/department participation, folder-access tri-state, people-search keyboard + email exclusion, schema vocabulary pins) plus the new static module above.

## Packet / register acceptance

- `python3 LoreForge_Execution_Packet/tools/validate_packet.py` → **PACKET VALIDATION: PASS** (84 ticket files, 69 implementation/design, 15 gates, 15 phases, references complete).
- Manifest counts unchanged: no new ticket files were added.
- Register rows all OPEN with named owners; every `DEF-*` ID now traceable into its owning ticket/path (newly: `DEF-AUTH-02`, `DEF-AUTH-03`, `DEF-FORM-01`, `DEF-PUBLIC-01`, `DEF-SEARCH-02`, `DEF-SEARCH-01`, `DEF-FTS-01`, `DEF-TENANT-01`, `DEF-ORIGIN-01`).
- Share is visibly and technically a placeholder (disabled UI control + 403-only endpoint, both pinned by regression).

## Notes / deviations

- The `document-shares` route comment text mentions "revoked"/"sharing" in prose; the regression asserts on code paths (no Payload import, no adapter import, no mutation calls), not comment vocabulary.
- `DEF-SHELL-01` persona rendering remains manual by design; the module pins everything statically assertable.
