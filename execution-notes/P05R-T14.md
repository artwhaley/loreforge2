# P05R-T14 — Safe development-database upgrade

**Status:** executed and verified

## Commands and evidence

- Read-only preflight: `DATABASE_URI=file:./sl-civic-archive.db node --import tsx src/scripts/upgradePhase5Review.ts --dry-run`
- Old-schema populated-copy rehearsal: copied the old database with SQLite `VACUUM INTO` to `sl-civic-archive-t14-fixture2.db`, then ran `DATABASE_URI=file:./sl-civic-archive-t14-fixture2.db node --import tsx src/scripts/upgradePhase5Review.ts --apply`.
- Idempotence rehearsal: ran the same apply command a second time against the fixture copy; it reported the same counts/fingerprints and no repairs.
- Actual apply (server quiesced): `DATABASE_URI=file:./sl-civic-archive.db node --import tsx src/scripts/upgradePhase5Review.ts --apply`
- Post-apply verifier: `DATABASE_URI=file:./sl-civic-archive.db node --import tsx src/scripts/upgradePhase5Review.ts --dry-run`

The actual preflight found no blockers: 0 null Folder destinations, 0 PermissionRule rows needing identity repair, and 1 valid supersession edge. The actual database contained 6 Documents, 117 Folders, 2 Users, 14 Characters, 18 DomainMemberships, 11 Roles, 8 Media rows, and 1 DocumentRelationship. All counts and stable content fingerprints were unchanged after the upgrade.

The verified consistent backup is:

`C:\Users\artwh\Downloads\loreforge2\sl-civic-archive\.loreforge-backups\sl-civic-archive-P05R-T14-20260904090821365.db`

The backup was created with SQLite `VACUUM INTO`, reopened read-only, and passed `integrity_check` and `foreign_key_check`. It is ignored by Git. No historical audit rows were invented. Legacy relationship lock ownership remains conservative (`lock_applied=0`, `prior_lifecycle=NULL`); the existing locked lifecycle was not changed.

## Result

The upgrade added Folder `public_access`, the append-only `domain_audit_events` table and indexes, PermissionRule `rule_key` plus storage uniqueness, relationship lock-origin fields and supersession uniqueness indexes, and the complete supporting indexes. Document Folder assignment is enforced at the application schema and by database triggers; all existing Documents already had a valid Folder, so no backfill was required. Payload opened the upgraded database with `PAYLOAD_PUSH=false`, and the schema verifier reported no errors.

