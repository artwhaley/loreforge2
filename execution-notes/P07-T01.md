# P07-T01 — Authorization rule model and deterministic evaluator

**Status: executed; automated evidence recorded.**

## Implemented

- Added a single evaluator for Platform Admin, Domain Owner/operational Admin,
  Personal owner, active Character membership, User/Character/Role/
  DomainMembership principals, resource ancestry, exact precedence, explicit
  deny, and default-deny behavior.
- Added resource-tree and role-tree services with senior-role inheritance and
  no Folder scope on RoleAssignments.
- Added explanation output and `requirePermission`/`isAllowed` seams for API,
  UI, and diagnostic consumers.

## Evidence

- Evaluator precedence mock: 1 passed.
- Role ancestry test: 1 passed.
- `npx tsc --noEmit` — passed.

## Manual acceptance

Deferred to the combined Phase 6/7 instructions because the final UI wiring is
completed in P07-T02 through P07-T05.

