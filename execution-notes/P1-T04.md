# P1-T04 — Design-owned config and stray-source cleanup

## Starting commit

`a49cabed490576589a887734768e06c7b8d6680b`

## Files changed

- `src/designs/{civic,ledger,poster,obsidian}/config.ts` (config type ownership)
- Design components/tests/indexes updated to import local config types
- `src/lib/design/contracts.ts` (generic-only contract surface)
- `src/lib/design/hostActionBridges.ts` (neutral production host bridge)
- `src/designs/obsidian/ObsidianOperationalSurfaces.tsx`
- `src/designs/obsidian/ObsidianDocumentTypes.tsx`
- removed `src/components/invitations/ObsidianActions.ts`
- removed `src/components/documentTypes/ObsidianActions.ts`

## Result

`CivicConfigV1`, `LedgerConfigV1`, `PosterConfigV1`, and `ObsidianConfigV1` now live in their Design folders and are no longer exported from generic `src/lib/design/contracts.ts`. Generic contracts retain shared asset/font/validation/theme/editor types. The Obsidian-specific action aliases were removed from components and the underlying generic action signatures are exposed through a neutral production host bridge, keeping authorization/server-action truth outside Designs while keeping first-class Design folders free of direct action imports.

## Commands/tests

- `npx tsc --noEmit` — passed.
- `npm run test:design` — passed, 24 files / 192 tests.

## Deviations / pressure

The existing first-class isolation test forbids direct `@/lib/actions/*` imports inside a Design, so the packet's allowed “move the alias into the folder” option would have violated the landed production seam. The neutral host bridge preserves the production action API and keeps the Design folder portable without weakening that invariant.

## Unresolved issues

Obsidian default asset URLs still use the old `/media/...`/`/designs/...` production paths until P1-T05. The action bridge will be part of the Lab host-dependency inventory in P1-T06.

## Next ticket

P1-T05 is safe to begin.

## Ending commit

`a49cabed490576589a887734768e06c7b8d6680b` (uncommitted working-tree changes; no commit created).
