# P08X-T01 — Rename and reorient: "Document Types" is the management entry

Status: complete

## What shipped

- `TenantShell` management nav: "Templates & Forms" → **"Document Types"**,
  href `${base}/forms` → `${base}/document-types`. Visibility now gates on
  `manage_types_tags` OR `manage_templates` (previously templates-only, which
  hid the entry from type-only managers).
- `/domain/[slug]/document-types`, `/templates`, `/forms` sub-navs: aria-label
  "Document Types", links ordered Document Types first (current page), then
  Templates, then Forms.
- Template/Form editor breadcrumbs (`/templates/new`, `/templates/[id]/edit`,
  `/forms/new`, `/forms/[id]/edit`): parent link now "Document Types" pointing
  at `/domain/[slug]/document-types`.
- Human-readable `title` hovertext added on every relabeled link.

## Carry-in (per owner commit-strategy decision)

The owner approved per-ticket commits that include pre-existing uncommitted
WIP when consistency requires it. This commit therefore also carries:

- the WIP `canDepartments` nav hunk in `TenantShell.tsx` (same file),
- the WIP "Create template"/Edit-link hunk in `templates/page.tsx` (same file),
- the untracked template-editor surface it touches and depends on:
  `templates/new/`, `templates/[templateId]/`, `components/templates/`
  (`DocumentTemplateForm`), and the WIP `createDocumentTemplateAction` /
  `updateDocumentTemplateAction` additions in `lib/actions/templates.ts`.

## Verification

- `npx tsc --noEmit`: clean
- Nav change is label-only; no behavior change beyond vocabulary (guardrail).

## Flags for owner

- The standalone Templates index page stays for now (ticket text flags it for
  retirement once T03 lands — owner decision).