# P04-T01 execution note

- Scope: Domain-scoped Document Types with seeded Plain Text, filing-policy precedence, four-state lifecycle rules, required Document Type, and lifecycle-aware new-record entry.
- Automated checks: `npm test` passed (52 tests); `npx tsc --noEmit` passed; Payload types regenerated.
- Manual acceptance: deferred to the Phase 4 gate after versions/provenance/workflow controls are present.
- Data note: seed now creates one Plain Text type per Domain and backfills existing records without changing their Markdown bodies.
