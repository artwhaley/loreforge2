# P07-T04 — Folder inheritance, direct grants, and explicit denies

Status: implemented.

## Delivered

- Added evaluator-backed `resolveFolderPermission` / `resolveFolderAccessTree` seams for the People and folder-centered views; both return the same read/write explanation from the shared resource ancestry evaluator.
- Folder Read and Write remain independent. Write is evaluated as document editing authority and route writes atomically maintain the `create_document` + `edit_document` pair.
- Parent grants flow to descendant folders without copied rows; a more-specific Character deny wins over an inherited Role grant.
- Direct Character, User, and Role Folder rules are protected by `assertCanDelegate`; grant requires `manage_access` and possession, while deny/revoke requires `manage_access`.
- Rule changes continue through the durable Domain audit seam and do not touch RoleAssignments.

## Verification

- Explicit deny/inherited Role grant regression passed.
- `npm test` — 89 passed, 0 failed.
- `npx tsc --noEmit` — passed.

Manual checks for inherited grant, explicit deny, and independent Read/Write edits are in `PHASE_06_07_TESTING.md`.
