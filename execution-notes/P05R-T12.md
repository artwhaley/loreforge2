# P05R-T12 — Atomic permission controls and durable audit

**Finding:** R04 and R09.
**Commit:** PENDING.

The Folder permission route now runs Read/Write replacement and its durable audit in one real transaction. Invalid axis states fail instead of silently doing the other half. Role creation/archive, RoleAssignment batches, and membership add/reactivate routes now join business mutations and audit writes to a transaction and return `error=mutation` on failure; audit errors are no longer swallowed as successful redirects.

`npx tsc --noEmit` is clean after the route changes. The People workspace regression (which exercises the permission route) passed **6/6** on an isolated pushed schema. A combined two-file run was not used as acceptance because both DB-backed test files delete/recreate their own database concurrently; each suite is intentionally run in a separate command/database. Full route failure-injection and batch-atomicity tests remain T15 work.
