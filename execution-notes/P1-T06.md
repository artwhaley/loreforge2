# P1-T06 — Boundary and key cleanup

- Repository: `sl-civic-archive`
- Branch: `patch/design-contract-management-and-obsidian`
- Start/end commit: `a49cabed490576589a887734768e06c7b8d6680b` (working tree execution; no commit created)

## Changes

- Replaced the legacy hard-coded Design key validator with generated `isDesignKey` data.
- Made legacy template metadata and Payload selector options derive from the generated catalog.
- Removed Design-key branches from the transitional legacy variant projection.
- Moved Obsidian bundled fonts into `src/designs/obsidian/assets/fonts/obsidian/` and changed CSS references to `/design-assets/obsidian/...`.
- Added `scripts/audit-design-packages.mjs` and the checked-in `docs/design-portability/host-dependencies.json` report.
- Added generated-registry metadata agreement checks so manifest metadata cannot silently disagree with `index.ts`.
- Kept the historical V2 migration adapter safe for later keys by falling back to Civic migration behavior while allowing the new key to resolve its own defaults.

## Verification

- `npm run design:audit`
- `npm run design:audit:check`
- `npx tsc --noEmit`

All passed. Generic host imports and static `/api/...` endpoints are documented in the report; no cross-Design import, folder escape, or unsanctioned asset reference was found.
