# P05R-T10 — Complete creation transaction and lock restoration

**Finding:** R02 and R07.
**Commit:** PENDING.

Transaction requests now flow through relationship/document hooks and document links. Application editor and Markdown-import creation paths use a real transaction; required Prepared-by, concerns/tags, relationship, and provenance writes are in the same unit. The shared transaction helper fails closed when the adapter does not provide a transaction instead of executing sequentially. Supersession edges record whether this operation applied the predecessor lock and the prior lifecycle; correction preserves an independently Locked predecessor.

## Verification

`DATABASE_URI=file:./sl-civic-archive-t10-test.db PAYLOAD_PUSH=true node --import tsx --test src/lib/documents/supersession.test.ts`

Result: **12 tests, 12 passed, 0 failed**. Coverage includes transaction-local create-and-relate, rollback, Draft/Pending rejection, invariants, filed success, originally Locked correction, and consecutive no-fork protection. The isolated database was removed after the run.

The two-attempt concurrent process test and injected post-step editor action failures remain T15 acceptance work. The current test directly exercises the production relationship/transaction service; T15 must additionally invoke the production editor action with a controlled request context.
