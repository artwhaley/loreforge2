# P05R-T15 — Production-path regression gate

**Status:** executed; evidence recorded for GATE2 review

## Evidence

- `npm test`: **85 passed, 0 failed**.
- `npm run test:security`: **all chained suites passed**: access boundary 4/4, supersession 12/12, independent concurrency 1/1, People workspace 6/6, Domain removal/audit 7/7, exhaustive >500 cleanup 1/1.
- `src/lib/documents/supersessionConcurrency.test.ts`: two independent Payload connections are released by a start barrier; exactly one transaction commits, the loser receives the SQLite lock conflict, and the committed edge/lock/provenance aggregate is verified through a fresh SQLite connection.
- `npx tsc --noEmit`: passed.
- `npm run lint`: passed with the two existing Next `<img>` optimization warnings in `TenantShell.tsx`; no errors.
- `npm run generate:types`: passed with no generated-type diff.
- Isolated production build: `DATABASE_URI=file:./sl-civic-archive-t14-fixture2.db PAYLOAD_PUSH=false PAYLOAD_SECRET=dev-only-secret-not-for-production-use npm run build` passed; all routes compiled.
- Packet validator: `python LoreForge_Execution_Packet/tools/validate_packet.py` — **PASS**, 84 ticket files, 15 gates, references complete.
- T14 schema preflight after apply: no schema errors or blockers; integrity and foreign-key checks passed.

## Corrective details

The security fixture setup now removes SQLite `-wal`, `-shm`, and `-journal` sidecars as well as the main throwaway file. This prevents stale WAL state from producing false file-level failures when the security suites run sequentially. The gate also includes a durable-audit-storage failure test and the >500-rule cleanup fixture.

## Limitations / owner review

Rendered UI behavior, visual workflow quality, owner acceptance of the current fixture, and the explicit Character-authored document-credit decision remain human review items for `P05R-GATE2`. Share remains intentionally deferred. No UI control exists for a supersession correction; its sanctioned service path is covered by automated tests.

