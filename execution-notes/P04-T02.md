# P04-T02 execution note

- Scope: Durable document revisions, lifecycle-aware editing, same-document revision history, read-only historical previews, and scoped restore-as-new-current behavior.
- Automated checks: `npx tsc --noEmit` passed after implementation. Full test suite reruns at the Phase 4 gate.
- Security invariant: history and restore resolve the current Document through the selected Domain, then verify the opaque Payload version's `parent` matches that Document before preview or restore.
- Manual acceptance: deferred to the Phase 4 gate. Human checks must verify that edits create revisions, pending/locked records are visibly read-only, cross-record version IDs are rejected, and restore adds a new revision.
