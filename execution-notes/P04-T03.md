# P04-T03 execution note

- Scope: Append-only Document provenance events, a single provenance writer, idempotent seed backfill, and a unified clerk-readable timeline alongside revision history.
- Automated checks: `npm test` passed (55 tests); `npx tsc --noEmit` passed; Payload types regenerated after adding the collection.
- Covered event paths: current Domain document create, editor save, form generation, move, restore, and seed-origin backfill. Lifecycle/review/lock actions are wired in P04-T04.
- Security invariant: normal collection create/update/delete/read access is denied; the application writer uses explicit Local API override only after the owning mutation succeeds. Timeline queries scope by both Domain and Document and use stable `occurredAt` + `id` ordering.
- Manual acceptance: deferred to the Phase 4 gate. Human checks must verify the clerk wording, actor display, revision links, and one coherent timeline through review/lock/unlock.
