# P06-T04 — Form-driven Document creation

**Status: executed; automated evidence recorded.**

Corrective follow-up (committed in `f46c71b`) closes the document-entry
guardrails that were previously listed as deferred.

## Implemented

- Unified `/domain/:domain/records/new` now loads active Templates, keeps the
  searchable Template/Type/destination/title/Prepared by/Concerns/Tags shell,
  and switches its body area between Markdown authoring and neutral-schema
  form fields.
- Template selection controls the Document Type and normal destination;
  fixed destinations reject forged alternatives and override-enabled Templates
  are checked against availability scope.
- Form answers are validated in memory, rendered to canonical Markdown with
  escaping, Character fields become typed Concerns links, the acting Character
  remains the Prepared-by credit, and Document/provenance/link work is atomic.
- Effective lifecycle policy now includes Template > Folder > Type > Domain;
  no raw structured answer object is stored.
- Added the safe direct SQLite schema/migration seam and migrated the actual
  development database's Incident Report Form into a neutral Form Template.
- Closed the P05R-T08 document-entry guardrails: Tags now use a searchable
  existing/create-new chip picker, and the creation surface can add extra
  active Domain Character Prepared-by credits while retaining the acting
  Character as a non-removable credit. Tag and link mutations are evaluator-
  backed by Document edit authority after creation.

## Evidence

- `npm run migrate:phase6-templates` with `PAYLOAD_PUSH=false` — imported 1,
  skipped 0; database integrity check passed.
- Neutral form/template test set: 13 passed.
- `npx tsc --noEmit` — passed.
- `npm test` — passed (90 tests).
- `npm run lint -- --no-cache` — passed (0 errors; 2 deliberate image warnings).
- `npm run build` — passed.

## Manual acceptance

Not run before unattended execution. The exact form-document walkthrough is in
`PHASE_06_07_TESTING.md`, including the acting/non-removable Prepared-by
credit, additional credit picker, searchable/create-new tags, and preserved
failure state.
