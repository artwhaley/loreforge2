# P03-T02 execution note

- Added `subdomains` and `subdomain-memberships` with one-Domain ownership, unique membership, Character head/admin assignments, and no recursive parent field.
- Added server-validated interim membership mutation routes using `authorizeInterimOperation`; ordinary users and forged requests are rejected by the server boundary.
- Added app-owned Domain member roster showing Character, Domain-local alias, controlling User, Domain membership, and a separate Subdomain-management column.
- Added Subdomain listing and landing pages with placeholder folder/template/recent-record sections and independent Subdomain membership controls.
- Seeded Ar with Scribes, Warriors, and Magistrates plus Character memberships; Bayview remains a contrasting Community Domain fixture.
- `npm run seed` passed with `PAYLOAD_PUSH=false` after schema creation; existing content remained intact and Ar fixtures were added idempotently.
- `npm test`: 45 passed.
- `npx tsc --noEmit`: passed.
- Manual acceptance remains for the Phase 3 gate: walk Ar as the seeded Character fixtures and exercise owner membership edits in the roster.
