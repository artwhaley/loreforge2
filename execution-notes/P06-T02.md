# P06-T02 — Neutral form schema and migration seam

**Status: executed; automated evidence recorded.**

## Implemented

- Added validated, versioned `LoreForgeFormSchema` supporting text,
  textarea, date, select, checkbox, and Character fields.
- Added the Payload Form Builder subset adapter with explicit unsupported-field
  warnings.
- Routed customer form filling and generation through `templates` and the
  neutral schema; legacy Form Builder submissions are closed for direct
  create/read/update/delete access.
- Added strict token rendering/Markdown escaping and an idempotent migration
  command (`npm run migrate:phase6-templates`) that copies only safe neutral
  data from the spike records.

## Evidence

- Neutral schema, adapter, composition, resolver, and generation tests: 13
  passed.
- `npx tsc --noEmit` — passed.
- Payload types regenerated with `npm run generate:types`.

## Manual acceptance

Not run before unattended execution. The combined clean-user workflow is in
`PHASE_06_07_TESTING.md`.

## Commit

`c9d73b8 P06-T02: add neutral form schema and migration seam`

