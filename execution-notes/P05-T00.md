# P05-T00 execution note

Status: implemented; automated checks passed. Manual owner acceptance remains at the Phase 5 gate.

## Decisions applied

- RoleAssignment is now only Character -> Department-owned Role. The rejected Folder scope and direct Department-membership write path are gone.
- Department rosters are derived from active RoleAssignments and active Domain membership.
- Direct Character Folder Read/Write overrides are stored as independent PermissionRules. Write is represented atomically by `create_document` plus `edit_document`; `inherit` removes direct rows. Effective access remains explicitly provisional until P07.
- People management now uses a server-ranked, debounced, keyboard-operable Character search and a Character workspace with separate Role and Folder controls.
- A repeatable `migrate:phase5` script removes legacy SubdomainMembership rows and maps the old fixture role names without reviving Folder scope.

## Verification

- `npm test` — 64 passed, 0 failed.
- `npx tsc --noEmit` — passed after regenerating types and clearing stale Next route types.
- Local HTTP/manual browser verification is deferred to the Phase 5 gate because the new PermissionRules table requires one schema push/restart on the existing SQLite fixture.

## Deferred

- Final authorization/effective-access evaluation remains P07 work, as required by the packet.
